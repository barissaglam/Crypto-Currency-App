package barissaglam.cryptocurrencyapp.utils

interface BundleKeys {

    object CoinDetail {
        const val KEY_UUID = "uuid"
    }

    object CoinList {
        const val KEY_COINS = "KEY_COINS"
    }
}
